file 'MyFile' [
    def int applyAction (User user, Proizvodi proizvodiNaAkciji, Porudzbina p){
        int id = 1;
        int tekila = 0;
        proizvodiNaAkciji.for( e=> {
            tekila = tekila + #p.proizvodi#.count(id==id);
            });
        if tekila > 3 {
            p.ukupnaCena = p.ukupnaCena - p.prooizvodi.min(cena);
        }
    }
]
